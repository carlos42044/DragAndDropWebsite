// For devlopment purposes
This website uses live.js to detect local file changes and automatically refreshes web page to see changes 
without hittin ctrl+r.

Must run a python server, if using python3 the command is: python3 -m http.server
